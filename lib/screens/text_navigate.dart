import 'package:flutter/material.dart';

class TestNavigate extends StatelessWidget {
  const TestNavigate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Test Success'),
        centerTitle: true,
      ),
    );
  }
}
